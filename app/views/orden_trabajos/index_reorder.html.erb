<div class="container mt-4">
  <h3 class="mb-4 text-center">🔀 Reordenar Órdenes por Máquina</h3>

  <% OrdenTrabajo::POST.each do |proceso| %>
    <%= render "reorder_maquina", proceso: proceso %>
  <% end %>

  <div class="text-center mt-4">
    <%= link_to "⬅ Volver al listado principal",
                orden_trabajos_path,
                class: "btn btn-secondary" %>
  </div>
</div>
