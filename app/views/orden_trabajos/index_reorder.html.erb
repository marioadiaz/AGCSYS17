<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4 px-3 flex-wrap">
  <div class="me-2">
    <h3 class="mb-2 text-center">🔀 Reordenar Órdenes por Máquina</h3>
  </div>
  
  <div class="ms-2">
      <%= link_to "Descargar PDF",
              index_reorder_orden_trabajos_path(format: :pdf),
              class: 'btn btn-success btn-lg',
              target: "_blank" %>
  </div>
</div>
  <% OrdenTrabajo::POST.each do |proceso| %>
    <%= render "reorder_maquina", proceso: proceso %>
  <% end %>

  <div class="text-center mt-4">
    <%= link_to "⬅ Volver al listado principal",
                orden_trabajos_path,
                class: "btn btn-secondary" %>
  </div>
</div>
